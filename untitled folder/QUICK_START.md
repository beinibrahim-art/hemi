# طريقة التشغيل المختصرة 🚀

## الطريقة السريعة (موصى بها)

```bash
# 1. انتقل إلى مجلد المشروع
cd "untitled folder"

# 2. شغّل السكريبت (سيقوم بكل شيء تلقائياً)
./start_enhanced.sh
```

**هذا كل شيء!** السكريبت سيقوم بـ:
- ✅ إنشاء البيئة الافتراضية
- ✅ تثبيت جميع المكتبات
- ✅ تشغيل التطبيق

---

## الطريقة اليدوية (إذا فشلت الطريقة السريعة)

```bash
# 1. إنشاء البيئة الافتراضية
python3 -m venv venv

# 2. تفعيل البيئة
source venv/bin/activate  # Linux/macOS
# أو
venv\Scripts\activate  # Windows

# 3. تثبيت المكتبات
pip install -r requirements.txt

# 4. تشغيل التطبيق
python3 app.py
```

---

## بعد التشغيل

افتح المتصفح على: **http://localhost:5000**

---

## ملاحظات سريعة

- **لإيقاف التطبيق**: اضغط `CTRL+C` في Terminal
- **إذا فشل تثبيت lxml**: لا تقلق، التطبيق يعمل بدونها
- **إذا فشل تثبيت Whisper على Python 3.13**: استخدم `faster-whisper` (موجود في requirements.txt)

---

## حل المشاكل الشائعة

### مشكلة: Permission denied
```bash
chmod +x start_enhanced.sh install_requirements.sh
```

### مشكلة: Python غير موجود
```bash
# macOS
brew install python3

# Linux
sudo apt-get install python3 python3-pip python3-venv
```

### مشكلة: ffmpeg غير موجود
```bash
# macOS
brew install ffmpeg

# Linux
sudo apt-get install ffmpeg
```

---

## الملفات المهمة

- `start_enhanced.sh` - سكريبت التشغيل التلقائي
- `install_requirements.sh` - تثبيت المكتبات فقط
- `app.py` - التطبيق الرئيسي
- `requirements.txt` - قائمة المكتبات

---

**💡 نصيحة**: استخدم `start_enhanced.sh` دائماً - أسهل وأسرع!
